% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constraints_compile.R
\name{compile_filter_ir}
\alias{compile_filter_ir}
\title{Compile a candidate-set filter expression to a C++ friendly IR}
\usage{
compile_filter_ir(
  filter_expr = TRUE,
  factor_meta = NULL,
  factor_levels = NULL,
  tol = 1e-10,
  max_clauses = 2048L
)
}
\arguments{
\item{filter_expr}{Default \code{TRUE}. R expression (language object) or TRUE/FALSE.}

\item{factor_meta}{Default \code{NULL}. Named list of factor metadata; each entry list(kind = 'discrete'|'numeric', levels = ...).}

\item{factor_levels}{Default \code{NULL}. List of allowed values per factor; used to encode direct predicates and provide original numeric values for linear atoms.}

\item{tol}{Default \code{1e-10}. Numeric snapping tolerance and scaled runtime tolerance for linear comparisons.}

\item{max_clauses}{Default \code{2048L}. Maximum allowed DNF clause count.}
}
\value{
Validated, zero-based constraint IR version 1 for \code{ConstraintSet}.
}
\description{
Supported operators:
\itemize{
\item Boolean: !, &, &&, |, ||, parentheses
\item Comparisons: ==, !=, <, <=, >, >=
\item Membership: \%in\% with c(...)
\item Linear comparisons: (a1\emph{x1 + a2}x2 + ... + c) relop rhs
\item Special-case lowering: !(A == a & B == b & ...) becomes a single forbid atom
}
}
\details{
Semantics: expression evaluates to TRUE for allowed points.
The parser converts expressions to DNF (disjunctive normal form). If expansion reaches
\code{max_clauses}, compilation errors; if it gets large but remains under the cap, a warning
is emitted suggesting forbidden tuple tables.
}
\keyword{internal}
