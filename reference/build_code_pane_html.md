# Build Code Pane HTML

Build Code Pane HTML

## Usage

``` r
build_code_pane_html(state)
```

## Arguments

- state:

  Named list containing `input` and reactive helpers used to render the
  code pane.

## Value

HTML string for the code pane.
