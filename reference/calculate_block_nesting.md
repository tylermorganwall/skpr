# Determine Nesting Level of Blocks

Calculates if a block is fully nested within another block, and what the
highest level of nesting is. 1 indicates the block isn't nested within
another block–just within the intercept.

## Usage

``` r
calculate_block_nesting(blockgroups, blockstructure)
```

## Value

Vector of numbers indicating the hierarchy
