# Detects disallowed combinations in candidate set

Detects disallowed combinations in candidate set

## Usage

``` r
detect_disallowed_combinations(candidateset)
```

## Arguments

- candidateset:

  Data frame

## Value

Returns a logical
